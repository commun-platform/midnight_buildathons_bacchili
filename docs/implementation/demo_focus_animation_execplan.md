# Guide attention through the narrated demo

This living plan follows `.agents/PLANS.md`.

## Purpose / Big Picture

The user finds it difficult to locate the part of a slide that the narration describes. Produce an updated movie in which sequential frames, a subdued surrounding area, and short labels guide attention to the current card, diagram component, or GUI field. Preserve the approved Japanese Wallet use-case narration. Deliver a playable MP4 and update the presentation's embedded movie.

## Progress

- [x] (2026-09-12) Inspect the existing fifteen-scene movie, narration timing, slide layout, and media validation interfaces.
- [x] (2026-09-12) Define 63 phrase-linked focus cues across all fifteen scenes after inspecting exact-timestamp GUI and diagram frames.
- [x] (2026-09-12) Implement the focus renderer; preview UC sequence, Wallet lifecycle, and Sponsor scenes. Fourteen media tests pass.
- [x] (2026-09-12) Render the 315.667-second focused movie, validate unchanged audio/subtitles and all seventeen delivery checksums, embed the exact MP4 in PPTX, verify sixteen PDF pages, and update delivery links.

## Surprises & Discoveries

The first contact-sheet sampling pass used FFmpeg fps reduction, which samples near interval centres. Review sheets were regenerated by selecting exact source frame numbers to avoid assigning cues from misleading timestamps.

The existing movie contains real moving GUI recordings as well as static slides. A fixed frame throughout a GUI scene would point to the wrong element after a scroll. Its raw clips must be reviewed at several timestamps before defining focus cues. The prior Japanese narration and subtitles are already approved; a separate video overlay can preserve their timing and reuse the audio stream without another speech generation pass.

## Decision Log

Use a separate, reproducible focus edit on top of `.demo-output/local-demo-wallet-uc-20260911/bacchiri-local-demo-ja.mp4`. Define scene-relative timing, target rectangles, and short editorial labels in a JSON focus script. Use FFmpeg's ASS renderer (timed text and vector shapes) to animate outlines and dim the surrounding content, keeping the title, subtitles, and filming label legible. Review moving scenes and avoid placing a fixed annotation over a scrolling target. Save the new edition below `.demo-output/local-demo-focus-20260912/`, preserving the reviewed baseline. No production GUI behavior or Wallet flow changes are required.

## Outcomes & Retrospective

The focused movie is complete at `.demo-output/local-demo-focus-20260912/bacchiri-local-demo-ja.mp4`. It contains 63 timed cues across fifteen scenes, at 1920×1080 and 30fps, with a duration of 315.666667 seconds. The AAC audio stream and SRT bytes match the approved Wallet use-case source. All source recordings and screenshot hashes still match their capture evidence. The final movie decodes completely, and the sixteen-slide PPTX embeds its exact bytes; the PDF has sixteen pages and the deck renderer reports no layout warnings.

Fourteen media tests, repository portability, whitespace checks, updated relative links, and seventeen artifact checksums pass. Representative moving previews and all GUI cue entrance/middle/exit samples were inspected; cues pause around scrolling, intentional rejection, and modal transitions. A final fifteen-scene contact sheet confirms the delivered emphasis across cards, diagrams, and GUI footage. The movie SHA-256 is `07bd203bc0ede086de45093ca71c8438f1c6554e4feae269cfc7c72f50cf360b`.

No requested work remains. The approved Wallet narrative and all original source media are preserved. The lesson from this edit is that narration timing and the visible location of a GUI element need separate review; a stationary outline over an entire scrolling clip is insufficient.

## Context and Orientation

`tools/submission-media/build-local-demo-video.py` renders the baseline movie and records its fifteen-scene timeline in `edit-manifest.json`. `local-demo-story.json` contains Japanese narration. The baseline directory also contains raw GUI clips, captured stills, narration SRT files, and slide PNG files. `build-local-demo-deck.cjs` validates the completed movie and embeds it on slide sixteen. A focus cue means a short time interval during which one visible region is outlined and labelled. Coordinates use the final 1920×1080 movie canvas, so they must include the scaling and margins around the original browser viewport.

## Plan of Work

First extract timestamped contact sheets from all eight GUI scenes and compare the visible controls with the narration. Read the diagram slides at full resolution to map their components. Then create the focus script and a focused video renderer that validates cue timing and coordinates, produces animated annotations, preserves audio, and writes provenance for the baseline and overlay. Preview representative slide, diagram, and GUI intervals before rendering the full movie. Finally copy the unchanged capture evidence and narration into the new edition, rebuild its presentation with the focused movie, validate the resulting MP4/PPTX/PDF, and update the English and Japanese media guides.

## Concrete Steps

Work from the repository root. The new renderer will run as:

    python3 tools/submission-media/build-local-demo-focus.py --source-dir .demo-output/local-demo-wallet-uc-20260911 --output-dir .demo-output/local-demo-focus-20260912

It will use `tools/submission-media/local-demo-focus.json` by default. The presentation command is:

    NODE_PATH=/tmp/bacchiri-wallet-media-deps/node_modules node tools/submission-media/build-local-demo-deck.cjs --output-dir .demo-output/local-demo-focus-20260912/slides --frames-dir .demo-output/local-demo-focus-20260912/frames --video .demo-output/local-demo-focus-20260912/bacchiri-local-demo-ja.mp4

The development presentation dependencies and the Noto Sans CJK JP font are already installed. No external assets or speech generation are required.

## Validation and Acceptance

Verify that every narrative scene has focus cues, each cue stays within its scene, and a cue's label and frame match the visible target at its start, middle, and end. Captions and the filming label must remain readable. The focus renderer must reject stale source hashes, overlapping cues, and out-of-frame target geometry. Inspect representative rendered video frames and short moving previews. Decode the full MP4, verify identical audio and subtitle contents against the baseline, check the sixteen-page PDF and sixteen-slide PPTX, and compare its embedded movie with the delivered MP4. Run the media-specific tests, repository portability, and `git diff --check`.

## Idempotence and Recovery

The baseline source is read-only during this edit. Re-running writes only the new edition and the canonical presentation after validation. Keep timing and provenance in JSON files so the edit can be reproduced without manually editing video. Do not alter concurrent unrelated changes, production runtime files, existing recordings, or real-evidence acceptance rules.

## Artifacts and Notes

Deliver `.demo-output/local-demo-focus-20260912/bacchiri-local-demo-ja.mp4`, matching subtitles, focus timing and provenance, and the updated canonical Japanese PPTX/PDF. Record the actual duration, cue count, and validation results on completion.

## Interfaces and Dependencies

Use Python's standard library and installed FFmpeg/FFprobe for the video overlay. ASS annotations support vector rectangles, opacity, timed entrances, and text. The JSON focus script references existing scene IDs and uses seconds relative to each scene. Retain the existing edit-manifest fields required by `build-local-demo-deck.cjs`, adding explicit focus-edit provenance rather than claiming another GUI capture.

Revision note (2026-09-12): Created for the user's request to exploit motion and highlighting to make narrated targets easy to locate.

Completion note (2026-09-12): Delivered the focus edition, matching embedded presentation and PDF, validation records, and current delivery links.
